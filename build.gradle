/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Gradle plugin project to get you started.
 * For more details take a look at the Writing Custom Plugins chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.5/userguide/custom_plugins.html
 */

plugins {
    // Apply the Java Gradle plugin development plugin to add support for developing Gradle plugins
    id 'java-gradle-plugin'

    // Apply the Groovy plugin to add support for Groovy
    id 'groovy'
    id 'maven-publish'
    id "com.gradle.plugin-publish" version "0.10.1"
}

repositories {
    // Use jcenter for resolving dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

dependencies {
    // Use the awesome Spock testing and specification framework
    testImplementation 'org.spockframework:spock-core:1.3-groovy-2.5'
}

gradlePlugin {
    // Define the plugin
    plugins {
        greeting {
            // 这个id要与最终引用的id是一致的。
            id = 'cn.wps.dynamicdep'
            implementationClass = 'dynamicdep.DynamicdepPlugin'
        }
    }
}

// Add a source set for the functional test suite
sourceSets {
    functionalTest {
    }
}

gradlePlugin.testSourceSets(sourceSets.functionalTest)
configurations.functionalTestImplementation.extendsFrom(configurations.testImplementation)

// Add a task to run the functional tests
tasks.register('functionalTest', Test) {
    testClassesDirs = sourceSets.functionalTest.output.classesDirs
    classpath = sourceSets.functionalTest.runtimeClasspath
}

check {
    // Run the functional tests as part of `check`
    dependsOn(tasks.functionalTest)
}

// 应该执行publishMavenPublicationToMavenLocal， 对应的本地仓库地址是 /Users/[用户名]/.m2/repository
publishing {
    publications {
        maven(MavenPublication) {
            // 不加group 在跑publishToMavenLocal时会报 groupId can't be empty. (明明要指定group，错误信息确实groupid，很操蛋。 )
            group = 'cn.wps'
            groupId = 'cn.wps'
            artifactId = 'dynamicdep'
            version = '1.1'

            from components.java
        }
    }

    // 发布到mavenLocal是成功了，但是gradle引用的时候有问题。 mavenLocal貌似是以缓存的路径存在的，引用时并不会搜索这个路径

    // 自定义了maven地址，运行的命令就是 publicMavenPublicationToMavenRepository
//    repositories {
//        maven {
//            // 也可以自定义本地库的地址。
//            url = "D:\\custom_reposity"
//        }
//    }
}

// 下面两个task是将插件上传到gradle官网插件库中。
//gradlePlugin {
//    plugins {
//        greetingsPlugin {
//            id = 'com.wps.dynamicdep' //插件的id
//            implementationClass = 'dynamicdep.DynamicdepPlugin'
//        }
//    }
//}
//
//pluginBundle {
//    website = 'https://github.com/shenyongxing/dynamicDependency'
//    vcsUrl = 'https://github.com/shenyongxing/dynamicDependency'
//    description = 'subtitue project implmenetion with module' //插件描述
//    tags = ['dynamic dependency'] //搜索关键词
//    plugins {
//        greetingsPlugin {
//            // id is captured from java-gradle-plugin configuration
//            displayName = 'dynamic dependency gradle plugin'
//        }
//    }
//}
